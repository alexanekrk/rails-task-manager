<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <input type="checkbox" name="task">
    <label for="task"> <%= link_to task.title, task_path(task) %> </label>
  <% end %>
</ul>

<p> <%= link_to "Add a task", tasks_path %> </p>
